services:
  - type: web
    name: ssleague-new
    env: python
    plan: free
    runtime: python
    buildCommand: pip install -r requirements.txt && python init_db.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --worker-class eventlet
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: # Set this to your Render PostgreSQL External Database URL
      - key: WEB_CONCURRENCY
        value: 1
      - key: FLASK_APP
        value: app.py
      - key: FLASK_ENV
        value: production
      - key: FLASK_DEBUG
        value: false
      - key: SKIP_BUILD_DB_INIT
        value: false
